= simple_form_for [:admin, @service] do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :body
    = f.association :service_category

  .form-actions
    = f.button :submit
