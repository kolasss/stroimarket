.page-header
  %h1
    Продавцы

    %small.control-links
      -# = icon_link 'plus-sign', new_admin_user_path

%table.table.table-hover
  %tr
    %th
      Лого
    %th
      Имя
    %th
      email
    %th
      Телефон
    %th
      Дата создания
    %th

  - @store_profiles.each do |profile|
    %tr
      %td= image_tag profile.logo if profile.logo.present?
      %td= link_to(profile.name, admin_user_path(profile.user))
      %td= profile.email
      %td= profile.phone
      %td= l profile.created_at, format: :short
      %td.control-links
        = icon_link 'pencil', edit_admin_user_store_profile_path(profile.user, profile)
        = icon_link 'remove', admin_user_store_profile_path(profile.user, profile), data: { method: :delete, confirm: t('common.confirm_deletion') }

= paginate @store_profiles
