.ng-cloak
  %h1 Магазины

  .row
    .col-md-4(ng-repeat="store in stores")
      .thumbnail
        / %img(ng-src="{{store.cover.thumb.url}}")
        .caption
          %h3 {{store.name}}
          %p {{store.title}}
          %p
            %a.btn.btn-primary(href="#/stores/{{store.slug}}")
              Подробнее
